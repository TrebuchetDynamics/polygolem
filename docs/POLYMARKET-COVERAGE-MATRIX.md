# Polymarket Coverage Matrix

This matrix tracks the Polymarket surfaces polygolem exposes through the Go
SDK, CLI, docs, and tests. It is intentionally conservative: live order
execution gates remain documented gaps until they have local tests.

For the upstream view — what each official service is for and where its
documentation lives — see [POLYMARKET-APIS.md](./POLYMARKET-APIS.md).

| Surface | Capabilities | SDK | CLI | Docs | Tests |
|---|---|---|---|---|---|
| Gamma markets | Search, list markets, fetch market by ID/slug, enrich with CLOB data, scan read-only opportunity candidates | `internal/gamma`, `pkg/gamma`, `pkg/types`, `pkg/universal`, `internal/workflows/opportunities` | `markets search`, `markets markets`, `markets market`, `markets enrich`, `markets opportunities` | `README.md`, `docs/COMMANDS.md`, Starlight CLI/Gamma pages | Gamma `httptest` tests, opportunity workflow tests, external SDK boundary test, CLI command registration |
| Gamma taxonomy | Tags/categories, curated polymarket.com category feeds, series, comments | `internal/gamma`, `pkg/gamma`, `pkg/types`, `pkg/universal` | `markets tags`, `markets categories`, `markets series`, `markets comments` | `docs/COMMANDS.md`, Starlight CLI/Gamma pages | External SDK boundary test, CLI command registration, category mapping tests |
| CLOB public data | Order book, price, midpoint, spread, tick size, fee rate, last trade, price history, market list, condition lookup, token lookup | `internal/clob`, `pkg/clob`, `pkg/orderbook`, `pkg/types`, `pkg/universal` | `book *`, `exchange book`, `exchange tick-size`, `exchange market`, `exchange market-by-token`, `exchange markets`, `exchange price-history` | `README.md`, `docs/COMMANDS.md`, Starlight CLOB/CLI pages | CLOB `httptest` tests, public SDK boundary test, CLI command registration |
| CLOB account reads | L2 API key creation/derivation, EOA-bound API key creation (owner address ignored in validated V2 path), balance, update balance, orders, order by ID, trades | `internal/clob`, `pkg/clob`, `pkg/universal` | `exchange create-api-key`, `exchange create-api-key-for-address`, `exchange balance`, `exchange update-balance`, `exchange orders`, `exchange order`, `exchange trades` | `docs/COMMANDS.md`, Starlight CLOB/CLI pages | CLOB and universal `httptest` tests, public SDK boundary test, CLI command registration |
| Contracts | Polygon registry, deposit-wallet bytecode deployment status | `internal/rpc`, `pkg/contracts`, `pkg/relayer` compatibility wrapper | `wallet status`, `wallet deploy` preflight | `docs/CONTRACTS.md`, `docs/ONBOARDING.md`, `docs/SAFETY.md` | RPC bytecode tests, CLI status/deploy guard tests |
| CLOB cancellation | Cancel one order, cancel a batch, cancel all, cancel by market/asset | `internal/clob`, `pkg/clob`, `pkg/universal` | `exchange cancel`, `exchange cancel-orders`, `exchange cancel-all`, `exchange cancel-market` | `README.md`, `docs/COMMANDS.md`, Starlight CLOB/CLI pages | CLOB and universal `httptest` tests, public SDK boundary test, CLI command registration |
| CLOB placement | Deposit-wallet limit and buy/sell market/FOK order signing with CLOB V2 payload shape and optional builder attribution | `internal/clob`, `pkg/clob`, `pkg/universal` | `exchange create-order`, `exchange market-order`, `--builder-code` | `README.md`, `docs/COMMANDS.md`, Starlight CLOB page, `docs/SAFETY.md`, `fixtures/conformance/order_v2_poly1271.json` | CLOB order-signing and placement tests, conformance/golden vector tests, public SDK boundary test |
| CLOB builder fee keys | Create, list, and revoke CLOB builder fee keys for V2 order attribution | `internal/clob`, `pkg/clob`, `pkg/universal` | `exchange create-builder-fee-key`, `exchange list-builder-fee-keys`, `exchange revoke-builder-fee-key` | `docs/COMMANDS.md`, Starlight CLOB/CLI pages, `fixtures/conformance/builder_headers.json` | CLOB `httptest` tests, builder header conformance tests, public SDK boundary test, CLI command registration |
| Data API | Positions, closed positions, trades, activity, holders, value, markets traded, open interest, leaderboard, live volume | `internal/dataapi`, `pkg/data`, `pkg/types`, `pkg/universal` | `analytics positions`, `analytics closed-positions`, `analytics trades`, `analytics activity`, `analytics holders`, `analytics value`, `analytics markets-traded`, `analytics open-interest`, `analytics leaderboard`, `analytics live-volume` | `README.md`, `docs/COMMANDS.md`, Starlight Data/CLI pages | Public Data API client tests, external SDK boundary test, universal route tests, CLI command registration |
| Bridge | Supported assets, deposit address creation, deposit status, quotes; withdrawal/offramp dry-run and explicit unsupported submit error | `pkg/bridge` | `bridge assets`, `bridge deposit`, `bridge status`, `bridge quote`; no live withdrawal CLI | `README.md`, `docs/COMMANDS.md`, Starlight bridge guide, `docs/POLYGOLEM-ROADMAP-MATRIX.md` | Package examples, withdraw dry-run tests, CLI command registration and JSON-envelope tests, public SDK boundary test |
| RFQ | Typed RFQ request/quote/response models, validation, and explicit unsupported submit placeholder | `pkg/rfq` | Not exposed; live RFQ posting intentionally unsupported | `docs/POLYGOLEM-ROADMAP-MATRIX.md`, `fixtures/schemas/rfq_request.schema.json` | RFQ validation tests, schema contract test, public SDK boundary test |
| WebSocket market stream | Public market channel subscription, raw deduplicated payload callback, book/price/last-trade/tick-size/best-bid-ask/lifecycle dispatch, reconnect, dedup helpers, lifecycle/message/event-count stats | `internal/stream`, `pkg/stream`, `pkg/universal` stream constructor | `stream market --custom-features --stats`, `stream crypto --stats` | `README.md`, `docs/COMMANDS.md`, Starlight Stream/CLI pages | Local WebSocket SDK test, stats counter tests, public SDK boundary test, CLI command registration |
| Live market-data snapshots | Normalized latest best bid, best ask, spread, midpoint, tick size, last trade, and book levels per token | `pkg/marketdata`, `pkg/stream` | `prices live` | `docs/COMMANDS.md`, Starlight Stream/SDK/Orderbook pages | `pkg/marketdata` tracker tests, local WebSocket CLI test |
| WebSocket user stream | Authenticated user order/trade stream with lifecycle/message stats | `pkg/stream.UserClient` | `stream user --stats` | `docs/V2-PARITY.md`, generated CLI refs | Local WebSocket SDK test, workflow stats test, CLI command registration |
| Reconciliation report | Read-only status labels and conflict detection across CLOB order records, Data API positions/trades, relayer transaction state, and optional on-chain fills | `pkg/reconciliation`, `pkg/orderfills`, `pkg/orderresults` | Not directly exposed | `CONTEXT.md`, `docs/POLYMARKET-COVERAGE-MATRIX.md` | Reconciliation classifier tests, public SDK boundary test |
| Polygon deposit wallet | Derive, deploy, status, nonce, batch, approve, fund, onboard, transaction lookup | `pkg/relayer`, `internal/auth`, `internal/relayer`, `internal/rpc` | `wallet *`, `tx transaction` | `README.md`, `docs/SAFETY.md`, deposit-wallet docs, generated CLI refs | Relayer onboard `httptest`, public SDK boundary test, existing auth/deposit-wallet tests, CLI command registration |
| CTF split/merge/redeem helpers | Split/merge/redeem calldata, high-level split/merge dry-runs, readiness-gated submit-plan artifacts, redeem submission via relayer | `pkg/ctf`, `pkg/settlement`, `pkg/relayer` | `wallet redeem`, `wallet settlement-status`; split/merge live submit intentionally not exposed | `docs/SAFETY.md`, `docs/POLYGOLEM-ROADMAP-MATRIX.md`, `fixtures/schemas/ctf_operation_request.schema.json`, `fixtures/conformance/ctf_calldata.json` | CTF calldata tests, full calldata conformance tests, dry-run/readiness tests, settlement tests, schema contract test, public SDK boundary test |
| Public signer adapters | Local private-key signer seam, HTTP remote signer, provider-neutral KMS-style backend seam, provider-neutral Turnkey-style backend seam | `pkg/signers`, `pkg/signers/http`, `pkg/signers/kms`, `pkg/signers/turnkey` | Not directly exposed; CLI default remains local env-key signing | `docs/POLYGOLEM-ROADMAP-MATRIX.md` | Signer adapter tests, public SDK boundary test, repository hygiene test |
| Polymarket error normalization | Stable typed categories for rate limits, geoblock, auth rejection, tick-size mismatch, closed markets, insufficient funds, and upstream outages | `pkg/polyerrors` | Not directly exposed | `docs/POLYMARKET-COVERAGE-MATRIX.md` | Error normalization tests |
| Agent/OpenAPI surfaces | Read-only MCP manifest and handler seam, stdio MCP server, read-only OpenAPI 3.1 spec, typed capability map, generated compatibility contract, upstream docs drift checker, redacted diagnostics | `pkg/mcp`, `pkg/openapi`, `pkg/capabilities`, `pkg/upstreamdrift` | `debug`, `check-upstream llms`; `cmd/polygolem_mcp`, `cmd/polygolem_openapi` binaries | `docs/MCP-OPENAPI.md`, `docs/COMPATIBILITY.md`, `docs/UPSTREAM-DRIFT-RUNBOOK.md`, `docs/POLYGOLEM-ROADMAP-MATRIX.md`, generated CLI refs | MCP/OpenAPI tests, capability-map tests, compatibility-doc drift test, upstream-drift tests, debug tests, docs safety test, generated docs drift tests |
| Polygon wallet actions outside deposit wallet | EOA/proxy/Safe trading modes | Not supported for live trading | Blocked for new production accounts | Documented as unsupported | N/A |

## Current Notes And Gaps

- Authenticated user WebSocket support now exists in SDK and CLI form; keep any
  live usage read-only/inspection-only and credential-gated.
- Data API open-interest currently requires a token ID in the CLI. A future
  all-market variant should be added only after the response shape is captured.
- `docs/COMMANDS.md` and the Starlight CLI reference are generated from the
  Cobra command tree. Run `go run ./cmd/polygolem_docs` after changing CLI
  commands.
- The shared CLI v1 JSON envelope is implemented for success payloads, group
  command usage errors, not-implemented stubs, common auth failures, and
  protocol/transport/chain failures with structured `error.details` for
  upstream HTTP status/source where available.
